# Open questions

_Compiler-maintained. High-importance events that have not yet been promoted into a canonical wiki page._

- **2026-04-23 02:02:28Z** · `compile` · session_summary — Session-summarization pass (`src/compile/summarize-sessions.ts`) now emits a sentinel `session_summary` event (module:null, summary:null, importance:normal) for every `session_close` that fails to pro…
- **2026-04-23 02:02:14Z** · `compile` · session_summary — The session-summarization pass (`src/compile/summarize-sessions.ts`) now emits a sentinel `session_summary` event (`module: null`, `summary: null`, `importance: normal`) whenever a `session_close` pro…
- **2026-04-23 02:02:03Z** · `compile` · session_summary — Raw hook events (user_prompt, file_write, session_close) are intentionally thin — they record what happened, not why. On their own they are not useful for the LLM compile pass. To produce meaningful w…
- **2026-04-23 02:02:03Z** · `(unscoped)` · session_summary — npm trusted-publish via GitHub Actions with sigstore provenance requires `package.json` to include a `repository` field whose `url` matches the GitHub repo URL from the publish run's provenance bundle…
- **2026-04-23 02:01:49Z** · `(unscoped)` · session_summary — npm trusted-publish via GitHub Actions with sigstore provenance requires `package.json` to include a `repository.url` matching the GitHub repo URL exactly (e.g. `git+https://github.com/eriks-briedis/c…
- **2026-04-23 02:01:49Z** · `compile` · session_summary — The compile pipeline now ingests session-end transcript summaries alongside raw JSONL events. A new `src/compile/summarize-sessions.ts` module shapes transcripts into per-session summaries that feed t…
- **2026-04-23 02:01:30Z** · `compile` · session_summary — The compile pipeline now ingests session-end transcript summaries alongside raw JSONL events. A new `src/compile/summarize-sessions.ts` module, plus changes in `compile/llm.ts`, `compile/summarize.ts`…
- **2026-04-23 02:01:30Z** · `(unscoped)` · session_summary — npm trusted-publish via GitHub Actions with sigstore provenance requires `package.json` to include a `repository` field whose URL matches the GitHub Actions repo context exactly. Without it, publish f…
- **2026-04-23 02:01:15Z** · `(unscoped)` · session_summary — npm trusted-publish via GitHub Actions with sigstore provenance requires `package.json` to include a `repository` field whose URL exactly matches the GitHub repo the workflow runs in; otherwise publis…
- **2026-04-23 02:01:15Z** · `compile` · session_summary — The compile pipeline now ingests session-end transcript summaries alongside raw JSONL events. A new `src/compile/summarize-sessions.ts` module produces per-session summaries that feed the LLM pass, an…
- **2026-04-23 02:00:59Z** · `(unscoped)` · session_summary — npm trusted-publish via GitHub Actions with sigstore provenance requires `package.json` to include a `repository` field whose `url` exactly matches the GitHub repo of the publish run. Missing/empty `r…
- **2026-04-23 02:00:59Z** · `compile` · session_summary — The compile pipeline now ingests session-end transcript summaries alongside raw JSONL events. A new `src/compile/summarize-sessions.ts` module was added for this pass, and new event types `session_sum…
- **2026-04-23 02:00:42Z** · `(unscoped)` · session_summary — npm trusted-publish via GitHub Actions with sigstore provenance requires package.json to include a `repository` field whose `repository.url` exactly matches the GitHub repo URL derived from the publis…
- **2026-04-23 02:00:42Z** · `compile` · session_summary — The compile pipeline now ingests session-end transcript summaries in addition to raw JSONL events. A new `src/compile/summarize-sessions.ts` module shapes transcripts into summaries that feed the LLM …
- **2026-04-23 02:00:26Z** · `(unscoped)` · session_summary — npm trusted-publish via GitHub Actions with sigstore provenance requires package.json to include a `repository` field whose `url` exactly matches the GitHub repo URL of the publishing workflow, otherw…
- **2026-04-23 02:00:26Z** · `compile` · session_summary — The compile pipeline now ingests session-end transcript summaries in addition to raw JSONL events. A new `src/compile/summarize-sessions.ts` module was added to handle shaping those transcript summari…
- **2026-04-23 01:59:55Z** · `(unscoped)` · session_summary — npm trusted-publish (GitHub Actions with sigstore provenance) requires `repository.url` in package.json to exactly match the GitHub repo URL of the publishing workflow, or publish fails with E422 'Err…
- **2026-04-23 01:59:55Z** · `compile` · session_summary — The compile pipeline now consumes session-end transcript summaries alongside raw events. A new `src/compile/summarize-sessions.ts` pass produces `session_summary` and `learned_fact` event types from s…
